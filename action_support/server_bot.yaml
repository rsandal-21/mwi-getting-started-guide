---
version: v2
proxy_server: teleport.sandal.tech:444
onboarding:
  join_method: github
  token: github-bot
certificate_ttl: 10m
storage:
  type: memory
outputs:
  - type: identity
    destination:
      type: directory
      path: ./ssh_out
