---
version: v2
proxy_server: mycluster.cloud.gravitational.io:443
onboarding:
  join_method: github
  token: github-bot
certificate_ttl: 10m
storage:
  type: memory
outputs:
- type: kubernetes/v2
  selectors:
    - env: myenvlabel
  destination:
    type: directory
    path: ./k8s-out