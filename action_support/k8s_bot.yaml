---
version: v2
proxy_server: ${TELEPORT_PROXY}
onboarding:
  join_method: github
  token: github-bot
certificate_ttl: 10m
storage:
  type: memory
outputs:
  - type: kubernetes/v2
    selectors:
      - labels:
          env: ${ENV_LABEL}
    destination:
      type: directory
      path: ./k8s_out